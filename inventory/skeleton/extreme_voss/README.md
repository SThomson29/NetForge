# Extreme VOSS Skeleton

Skeleton host_vars files for Extreme VOSS devices will be added here once
the J2 templates have been built out. The provisioning script will use these
when a host belongs to a group prefixed with `voss_` or `voss`.
